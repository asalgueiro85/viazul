{% extends '::front_layout.html.twig' %}

 {% block nomencladores %} class="active" {% endblock %}
 {% block causa %} class="active" {% endblock %}


{% block body -%}
    <h1><i class="fa fa-check-square"></i> Historial de Acciones</h1>

    <div class="row">
        <div class="col-xs-12">
            <div class="box">
                <div class="box-header">
                    <h3 class="box-title">Acciones Realizadas</h3>
                </div>
                <!-- /.box-header -->
                <div class="box-body">


                    <table id="example1" class="table table-bordered table-striped">
                        <thead>
                        <tr>
                            <th>Fecha</th>
                            <th>Nombre</th>
                            <th>Importe</th>
                            <th>Descipción</th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for entity in entities %}
                            <tr>
                                <td>
                                    {{ entity.fecha |date('Y-m-d')}}
                                </td>
                                <td>
                                    {{ entity.nombre }}
                                </td>
                                <td>
                                    {{ entity.importe }}
                                </td>
                                <td>
                                    {{ entity.descripcion }}
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>


                </div>
                <!-- /.box-body -->
            </div>
            <!-- /.box -->
        </div>
        <!-- /.col -->
    </div><!-- /.row -->


{% endblock %}